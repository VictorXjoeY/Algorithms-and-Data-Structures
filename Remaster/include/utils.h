#ifndef UTILS_H
#define UTILS_H

#define min(a, b) (a < b ? a : b)
#define max(a, b) (a > b ? a : b)

#define and &&
#define or ||
#define not !

#define false 0
#define true 1

typedef unsigned char bool;

#endif